# AgentShield 0.9.0 Release Notes

Release date: 2026-08-14

## Scope

AgentShield 0.9.0 introduces automated remediation (`agentshield fix`), runtime guard MCP stdio proxy hardening, and expanded framework ecosystem adapters.

## Notable changes

- **Automated Remediation Engine (`agentshield fix`)**:
  - Implemented `FixEngine` and `FilePatch` for automated, safe codebase fixes.
  - Unified diff formatting in `--dry-run` preview mode.
  - Automated patching of unsafe deserializers (SHIELD-016): `yaml.load` rewritten to `yaml.safe_load` / `Loader=yaml.SafeLoader`, and `pickle.loads` rewritten to `json.loads`.
  - Automated dependency version pinning (SHIELD-009) for `requirements.txt` and `package.json`.
- **Runtime Guard MCP Stdio Proxy Hardening**:
  - Live JSON-RPC parameter inspection for parameter aliases (`cmd`, `filepath`, `target_url`) blocking private IPs and cloud metadata SSRF endpoints.
  - Outgoing response stream secret sanitization via `redact_text` in `mcp_proxy_stdio.rs`.
- **Expanded Framework Adapters**:
  - Direct ingestion of OpenAI Tool and Function calling schemas in `tools.json`, `functions.json`, `tools.yaml`, and `assistant.json`.
  - Added support for TypeScript and JavaScript LangChain tool definitions (`@langchain/core`, `langchain`, `langgraph`).

## Compatibility

- `agentshield fix` defaults to safe in-place modifications with `--dry-run` available.
- Runtime proxy changes are backward-compatible with MCP stdio transports.
- Console, JSON, SARIF, HTML, and DSSE output contracts remain backward-compatible.

## Readiness

- 549 unit and integration tests passing (`rtk cargo test --all-features`).
- Clippy passes with zero warnings (`rtk cargo clippy --all-features -- -D warnings`).
- Release invariants verified.
