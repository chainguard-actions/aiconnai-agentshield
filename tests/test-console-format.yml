name: Test console output format
on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Prepare fake agentshield binary (clean scan)
        run: |
          cp "$GITHUB_WORKSPACE/tests/fixtures/fake-agentshield-clean.sh" /tmp/fake-agentshield
          chmod +x /tmp/fake-agentshield

      - id: action
        uses: .
        with:
          path: '.'
          format: 'console'
          fail-on: 'high'
          upload-sarif: 'false'
          strict: 'true'
          binary-path: '/tmp/fake-agentshield'

      - name: Assert exit-code is 0
        env:
          EXIT_CODE: ${{ steps.action.outputs.exit-code }}
        run: |
          echo "exit-code: $EXIT_CODE"
          if [ "$EXIT_CODE" != "0" ]; then
            echo "FAIL: expected exit-code=0, got '$EXIT_CODE'"
            exit 1
          fi
          echo "PASS"

      - name: Assert sarif-file is empty (console format does not produce SARIF)
        env:
          SARIF_FILE: ${{ steps.action.outputs.sarif-file }}
        run: |
          echo "sarif-file: '$SARIF_FILE'"
          if [ -n "$SARIF_FILE" ] && [ -f "$SARIF_FILE" ]; then
            echo "FAIL: expected no SARIF file for console format, but found '$SARIF_FILE'"
            exit 1
          fi
          echo "PASS: no SARIF file produced for console format"

      - name: Assert finding-count is 0 (no SARIF to count from)
        env:
          COUNT: ${{ steps.action.outputs.finding-count }}
        run: |
          echo "finding-count: $COUNT"
          if [ "$COUNT" != "0" ]; then
            echo "FAIL: expected finding-count=0 for console format, got '$COUNT'"
            exit 1
          fi
          echo "PASS"
